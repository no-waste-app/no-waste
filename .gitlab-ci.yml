stages:
    - build
    - test
build: 
    stage: build
    image: node:14
    script:
        - echo "Building UI app"
        - cd ui
        - yarn install
        - yarn build
test:
    stage: test
    image: node:14
    script:
        - echo "Testing"
        - cd ui
        - yarn install
        - yarn lint
        - yarn test
