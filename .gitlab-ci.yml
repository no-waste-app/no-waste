stages:
    - build
    - test
build: 
    stage: build
    script:
        - echo "Building gui app"
        - cd no-waste-gui
        - npm run build
test:
    stage: test
    script:
        - echo "Testing"
        - cd no-waste-gui
        - npm test
